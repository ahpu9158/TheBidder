import type { ScoreBoardEntry } from '../../interface';

export function ScoreBoardEntry({ scoreBoardEntry }: { scoreBoardEntry: ScoreBoardEntry }) {
    return (
        <div className="overflow-x-auto">
            <div>
            </div>
        </div>
    );
}